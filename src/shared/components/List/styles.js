import { css } from "styled-components";

const styles = {
  container: css`
    display: flex;
    align-items: center;
    justify-content: center;
    margin: auto;
    text-align: center;
    flex-direction: column;
  `
};

export default styles;
