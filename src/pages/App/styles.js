import { css } from "styled-components";

const styles = {
  container: css`
    display: flex;
    height: 200px;
    align-items: center;
    justify-content: center;
  `
};

export default styles;
