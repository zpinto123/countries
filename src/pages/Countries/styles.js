import { css } from "styled-components";

const styles = {
  container: css`
    text-align: center;
    position: relative;
    display: flex;
    flex-direction: column;
  `
};
export default styles;
